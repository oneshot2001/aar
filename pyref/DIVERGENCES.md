# Gate 4 clean-room divergences

Generated by `python -m pyref.kat --slice c2`.

## C1

No C1 divergences recorded.

## C2 entry-point finding

CONFORMANCE section 5 defines a W-12 verdict over a bundle, while the C2 corpus also requires verdicts for standalone `aar-wire-object` fixtures. For those fixtures, the runner requires byte-identical membership in (or deterministically augments) the published positive bundle KAT, uses that bundle's trust/scope context, and binds `bundle_digest` to the exact standalone fixture bytes. This is a clean-room corpus-entry-point interpretation requiring gate adjudication.

## C2 verdict/reason divergences

### 1. `kats/class-boundary/provenance-provider-attested-satisfied.cbor`

- Expected: `{"class": "provider_attested", "code": null, "deterministic": true, "result": "conformant"}`
- Produced: `{"class": "not_evaluated", "code": null, "deterministic": true, "result": "conformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 19 recomputes the maximum declared and structurally supported evidence classes.

- Ambiguity hypothesis: The verdict result/reason agrees, but the C2 reporter selected the class dimension from the boundary's left-hand label (for example `proxy_captured_to_provider_attested`) and therefore reported the outcome dimension as `not_evaluated`. This is likely a pyref boundary-reporting bug, not a wire-verdict disagreement.

### 2. `kats/class-boundary/provenance-proxy-captured-satisfied.cbor`

- Expected: `{"class": "proxy_captured", "code": null, "deterministic": true, "result": "conformant"}`
- Produced: `{"class": "not_evaluated", "code": null, "deterministic": true, "result": "conformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 19 recomputes the maximum declared and structurally supported evidence classes.

- Ambiguity hypothesis: The verdict result/reason agrees, but the C2 reporter selected the class dimension from the boundary's left-hand label (for example `proxy_captured_to_provider_attested`) and therefore reported the outcome dimension as `not_evaluated`. This is likely a pyref boundary-reporting bug, not a wire-verdict disagreement.

### 3. `kats/class-boundary/time-boot-bound-satisfied.cbor`

- Expected: `{"class": "boot_bound", "code": null, "deterministic": true, "result": "conformant"}`
- Produced: `{"class": "not_evaluated", "code": null, "deterministic": true, "result": "conformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 19 recomputes the maximum declared and structurally supported evidence classes.

- Ambiguity hypothesis: The verdict result/reason agrees, but the C2 reporter selected the class dimension from the boundary's left-hand label (for example `proxy_captured_to_provider_attested`) and therefore reported the outcome dimension as `not_evaluated`. This is likely a pyref boundary-reporting bug, not a wire-verdict disagreement.

### 4. `kats/class-boundary/time-externally-anchored-satisfied.cbor`

- Expected: `{"class": "externally_anchored", "code": null, "deterministic": true, "result": "conformant"}`
- Produced: `{"class": "not_evaluated", "code": null, "deterministic": true, "result": "conformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 19 recomputes the maximum declared and structurally supported evidence classes.

- Ambiguity hypothesis: The verdict result/reason agrees, but the C2 reporter selected the class dimension from the boundary's left-hand label (for example `proxy_captured_to_provider_attested`) and therefore reported the outcome dimension as `not_evaluated`. This is likely a pyref boundary-reporting bug, not a wire-verdict disagreement.

### 5. `kats/negative/bundle-artifact-out-of-scope.cbor`

- Expected: `{"class": null, "code": "bundle/artifact-out-of-scope", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "bundle/range-boundary", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Steps 3, 7, and 18 validate selector commitments, dependencies, ranges, coverage, and scope.

- Ambiguity hypothesis: Pyref compares each carried range against every objective manifest entry in the half-open temporal slice before checking extra carried artifacts. The fixture appears to treat that range as boundary-complete under a narrower selector reading, while D-19 says ranges carry nonmatching kinds/subjects as well.

### 6. `kats/negative/credential-compromised.cbor`

- Expected: `{"class": null, "code": "credential/compromised", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "credential/status-missing", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 8 enforces credential paths, roots, rotations, status, compromise, and lease maxima.

- Ambiguity hypothesis: Changing and re-identifying the stapled snapshot leaves the clean-room reading of the decision's exact status_snapshot_id reference unresolved, making status-missing earlier than the intended status/lease defect. The fixture may intend the mutated snapshot to replace that reference implicitly, which the spec does not state.

### 7. `kats/negative/credential-lease-expired.cbor`

- Expected: `{"class": null, "code": "credential/lease-expired", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "credential/status-missing", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 8 enforces credential paths, roots, rotations, status, compromise, and lease maxima.

- Ambiguity hypothesis: Changing and re-identifying the stapled snapshot leaves the clean-room reading of the decision's exact status_snapshot_id reference unresolved, making status-missing earlier than the intended status/lease defect. The fixture may intend the mutated snapshot to replace that reference implicitly, which the spec does not state.

### 8. `kats/negative/credential-lease-too-long.cbor`

- Expected: `{"class": null, "code": "credential/lease-too-long", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "credential/status-missing", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 8 enforces credential paths, roots, rotations, status, compromise, and lease maxima.

- Ambiguity hypothesis: Changing and re-identifying the stapled snapshot leaves the clean-room reading of the decision's exact status_snapshot_id reference unresolved, making status-missing earlier than the intended status/lease defect. The fixture may intend the mutated snapshot to replace that reference implicitly, which the spec does not state.

### 9. `kats/negative/credential-revoked.cbor`

- Expected: `{"class": null, "code": "credential/revoked", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "credential/status-missing", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 8 enforces credential paths, roots, rotations, status, compromise, and lease maxima.

- Ambiguity hypothesis: Changing and re-identifying the stapled snapshot leaves the clean-room reading of the decision's exact status_snapshot_id reference unresolved, making status-missing earlier than the intended status/lease defect. The fixture may intend the mutated snapshot to replace that reference implicitly, which the spec does not state.

### 10. `kats/negative/credential-status-stale.cbor`

- Expected: `{"class": null, "code": "credential/status-stale", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "credential/status-missing", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 8 enforces credential paths, roots, rotations, status, compromise, and lease maxima.

- Ambiguity hypothesis: Changing and re-identifying the stapled snapshot leaves the clean-room reading of the decision's exact status_snapshot_id reference unresolved, making status-missing earlier than the intended status/lease defect. The fixture may intend the mutated snapshot to replace that reference implicitly, which the spec does not state.

### 11. `kats/negative/credential-status-unknown.cbor`

- Expected: `{"class": null, "code": "credential/status-unknown", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "credential/status-missing", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 8 enforces credential paths, roots, rotations, status, compromise, and lease maxima.

- Ambiguity hypothesis: Changing and re-identifying the stapled snapshot leaves the clean-room reading of the decision's exact status_snapshot_id reference unresolved, making status-missing earlier than the intended status/lease defect. The fixture may intend the mutated snapshot to replace that reference implicitly, which the spec does not state.

### 12. `kats/negative/delegation-chain-invalid.cbor`

- Expected: `{"class": null, "code": "delegation/chain-invalid", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": null, "deterministic": true, "result": "conformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 13 requires one dominating valid delegation whose scope contains the action flow.

- Ambiguity hypothesis: The mutated top-level delegation artifact is not the byte-identical delegation embedded in the carried authorization. Pyref applies attempt-time delegation checks to the embedded dominating delegation and treats the unreferenced top-level artifact as validly signed content; the fixture appears to require lifecycle/scope evaluation of every carried delegation even when it is not selected by an authorization path.

### 13. `kats/negative/delegation-expired.cbor`

- Expected: `{"class": null, "code": "delegation/expired", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": null, "deterministic": true, "result": "conformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 13 requires one dominating valid delegation whose scope contains the action flow.

- Ambiguity hypothesis: The mutated top-level delegation artifact is not the byte-identical delegation embedded in the carried authorization. Pyref applies attempt-time delegation checks to the embedded dominating delegation and treats the unreferenced top-level artifact as validly signed content; the fixture appears to require lifecycle/scope evaluation of every carried delegation even when it is not selected by an authorization path.

### 14. `kats/negative/delegation-not-yet-valid.cbor`

- Expected: `{"class": null, "code": "delegation/not-yet-valid", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": null, "deterministic": true, "result": "conformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 13 requires one dominating valid delegation whose scope contains the action flow.

- Ambiguity hypothesis: The mutated top-level delegation artifact is not the byte-identical delegation embedded in the carried authorization. Pyref applies attempt-time delegation checks to the embedded dominating delegation and treats the unreferenced top-level artifact as validly signed content; the fixture appears to require lifecycle/scope evaluation of every carried delegation even when it is not selected by an authorization path.

### 15. `kats/negative/delegation-scope.cbor`

- Expected: `{"class": null, "code": "delegation/scope", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": null, "deterministic": true, "result": "conformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 13 requires one dominating valid delegation whose scope contains the action flow.

- Ambiguity hypothesis: The mutated top-level delegation artifact is not the byte-identical delegation embedded in the carried authorization. Pyref applies attempt-time delegation checks to the embedded dominating delegation and treats the unreferenced top-level artifact as validly signed content; the fixture appears to require lifecycle/scope evaluation of every carried delegation even when it is not selected by an authorization path.

### 16. `kats/negative/epoch-late-insertion.cbor`

- Expected: `{"class": null, "code": "epoch/late-insertion", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "epoch/anchor-deadline", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Step 14 validates event chains, epoch transitions, duration, spans, deadlines, late arrivals, and forks.

- Ambiguity hypothesis: Pyref checked the close event's declared anchor deadline before comparing receipt commit time with the changed manifest close. Step 14 lists late-arrival routing before anchor deadline, so this is likely a pyref intra-step ordering bug exposed by a fixture carrying both consequences of the close-time mutation.

### 17. `kats/negative/manifest-index-duplicate.cbor`

- Expected: `{"class": null, "code": "manifest/index-duplicate", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "manifest/index-root-mismatch", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Steps 7, 15, and 19 validate manifest payloads, objective indexes, and required evidence artifacts.

- Ambiguity hypothesis: The changed signed index entry bytes no longer recompute to the carried root. Pyref follows step 15's literal order, 'Recompute entry leaves and root, then require sort order, contiguous leaf indices, unique ...', so it reports root mismatch before the fixture's intended later index defect.

### 18. `kats/negative/manifest-index-gap.cbor`

- Expected: `{"class": null, "code": "manifest/index-gap", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "manifest/index-root-mismatch", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Steps 7, 15, and 19 validate manifest payloads, objective indexes, and required evidence artifacts.

- Ambiguity hypothesis: The changed signed index entry bytes no longer recompute to the carried root. Pyref follows step 15's literal order, 'Recompute entry leaves and root, then require sort order, contiguous leaf indices, unique ...', so it reports root mismatch before the fixture's intended later index defect.

### 19. `kats/negative/manifest-index-order.cbor`

- Expected: `{"class": null, "code": "manifest/index-order", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "manifest/index-root-mismatch", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Steps 7, 15, and 19 validate manifest payloads, objective indexes, and required evidence artifacts.

- Ambiguity hypothesis: The changed signed index entry bytes no longer recompute to the carried root. Pyref follows step 15's literal order, 'Recompute entry leaves and root, then require sort order, contiguous leaf indices, unique ...', so it reports root mismatch before the fixture's intended later index defect.

### 20. `kats/negative/manifest-index-receipt-mismatch.cbor`

- Expected: `{"class": null, "code": "manifest/index-receipt-mismatch", "deterministic": true, "result": "nonconformant"}`
- Produced: `{"class": null, "code": "manifest/index-root-mismatch", "deterministic": true, "result": "nonconformant"}`
- Spec sentence(s) relied on:

  > First failure wins. No verifier may continue to a later step and substitute its reason for an earlier failure.

  > Steps 7, 15, and 19 validate manifest payloads, objective indexes, and required evidence artifacts.

- Ambiguity hypothesis: The changed signed index entry bytes no longer recompute to the carried root. Pyref follows step 15's literal order, 'Recompute entry leaves and root, then require sort order, contiguous leaf indices, unique ...', so it reports root mismatch before the fixture's intended later index defect.
