# Same-operator demo anchoring — local RFC 6962 v1 log

This append-only JSONL log uses the harness RFC 6962 leaf, root, and inclusion
proof code. It demonstrates ordering and inclusion under the same demo
operator. It is not independent timestamping, withholding resistance, or a
third-party transparency service.
