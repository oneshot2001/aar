# Mediator countersignature KATs

`mediator-countersign-absent.cbor` is byte-identical to
`kats/positive/bundle-valid-subset.cbor` by design. D-67 is optional and its
absence preserves the pre-D-67 bundle encoding exactly.
